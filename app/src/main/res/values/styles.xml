<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="drawerArrowStyle">@style/AppTheme.MyDrawerArrowStyle</item>
    </style>

    <!--自定义 navigation drarwer 的样式-->
    <style name="AppTheme.MyDrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">#FFFFFF</item>
    </style>

    <style name="AppTheme.ListTitle">
        <item name="android:background">@color/title_layout_background</item>
        <item name="android:padding">@dimen/list_title_layout_padding</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/list_title_layout_height</item>
    </style>
</resources>
